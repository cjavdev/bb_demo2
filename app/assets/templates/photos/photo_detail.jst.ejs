<h1><%= photo.escape('title') %></h1>
<a href="#/photos">All Photos</a>
<h3>Comments</h3>
<ul class="comments">
  <% photo.comments().each(function(comment) { %>
    <li><%= comment.escape('body') %></li>
  <% }) %>
</ul>
<img src="<%= photo.get('url') %>">
